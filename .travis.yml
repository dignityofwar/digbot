sudo: required

services:
  - docker

before_script:
    - docker-compose -f docker-compose-test-ci.yml up --build -d

script:
    - docker-compose exec node ./app/test.sh

after_script:
    - docker-compose down

notifications:
    email: false
